'use strict';

{
    console.log('this code is try_catch.js');

    const name = 45;

    try {
        console.log(name.toUpperCase());
    } catch (e) {
        console.log(e);
    }

    console.log('Finish!');
}
